# SGE QA 驗證 Skill - 缺失資訊分析

根據已提供的資料和構建的 SKILL，以下是 Agent 執行時需要但目前缺乏的資訊：

## 🔴 關鍵缺失（執行前必須補充）

### 1. 具體的後台數據結構和定位方式

**目前已知：**
- 後台各頁面的 URL
- 登入憑證

**缺失資訊：**
- ❌ 每個驗證項目在後台的確切位置導航步驟
  - 例如：如何在後台找到特定的 Banner ID？
  - 如何篩選和定位特定活動的資料？
  - 後台表格的欄位名稱是什麼？
  
- ❌ 後台頁面的 HTML 結構和元素識別方式
  - 資料表格的 CSS class 或 ID
  - 搜尋框的識別方式
  - 資料提取的選擇器

**建議補充方式：**
- 提供具體的後台操作截圖和步驟說明
- 記錄常見的後台頁面 HTML 結構範例
- 建立「確認方法」到後台操作的對應表

### 2. 驗證單的具體結構和格式

**目前已知：**
- 黃色表頭欄位是驗證項目
- 有「確認方法」、「確認項目」等欄位
- 需要填寫「OK」、日期、擔當等

**缺失資訊：**
- ❌ 驗證單的標準欄位順序和名稱
  - 各欄位在第幾列（Column A, B, C...）？
  - 不同分頁是否有不同的欄位結構？
  
- ❌ 如何識別「待驗證」vs「無需驗證」的儲存格
  - 白底的具體色碼是什麼？
  - 灰底的具體色碼是什麼？
  
- ❌ 紅底標記錯誤時的具體格式
  - 背景色的色碼
  - 是否需要同時設定文字顏色？
  - 錯誤說明文字的格式要求

**建議補充方式：**
- 提供完整的驗證單欄位對照表（欄位名稱 → Column 位置）
- 提供色碼對照表（白底、灰底、紅底、黃色表頭的確切色碼）
- 提供填寫範例的截圖

### 3. 資料比對的具體邏輯和規則

**目前已知：**
- 需要使用 TRIM() 移除空白
- 需要使用 VALUE() 處理數字

**缺失資訊：**
- ❌ 什麼情況算「符合」？
  - 是否需要完全一致（大小寫、標點符號）？
  - 數字比對是否有容錯範圍？
  - 日期格式的比對規則？
  
- ❌ 特殊欄位的比對規則
  - Bonus 資料的拆分規則（▸〈〉分隔符）
  - 複雜欄位的比對方式
  
- ❌ 多語言資料的比對
  - 日文、英文混合時如何處理？
  - 編碼問題的處理方式

**建議補充方式：**
- 提供資料比對規則文檔
- 列舉常見的「看起來不同但應判定為相同」的案例
- 說明特殊欄位的資料格式和解析方式

## 🟡 重要缺失（影響執行效率）

### 4. 具體的驗證案例和範例

**缺失資訊：**
- ❌ 完整的端到端驗證範例
  - 從拿到驗證單 URL 到完成驗證的完整流程錄影或截圖
  - 包含正常案例和異常案例
  
- ❌ 常見的驗證項目類型
  - Banner 驗證的標準流程
  - Card/Still 驗證的標準流程
  - Package Sale 驗證的標準流程
  - 等等...

**建議補充方式：**
- 錄製一次完整的人工驗證過程
- 整理「驗證項目類型」→「後台操作」→「比對方式」的對應表
- 建立常見問題的 FAQ

### 5. Google Sheet API 操作的細節

**缺失資訊：**
- ❌ 如何準確定位要更新的儲存格？
  - 使用 A1 notation 還是 R1C1？
  - 如何批量更新多個不連續的儲存格？
  
- ❌ 如何設定儲存格格式？
  - 背景色、文字顏色、邊框等
  - 使用 batchUpdate API 的具體參數
  
- ❌ 如何處理 Sheet 的保護範圍？
  - 是否有某些欄位是鎖定的？
  - 需要特定權限嗎？

**建議補充方式：**
- 提供 Google Sheet API 操作的範例程式碼
- 說明驗證單的編輯權限設定

### 6. 環境切換的具體機制

**目前已知：**
- 有測試環境（テスト環境）和正式環境（本番環境）

**缺失資訊：**
- ❌ 測試環境和正式環境的後台 URL 是否不同？
  - 如果不同，各是什麼？
  - 登入憑證是否相同？
  
- ❌ 驗證單中如何區分測試環境和正式環境的記錄？
  - 是在不同的欄位區塊嗎？
  - 還是在不同的分頁？

**建議補充方式：**
- 明確說明兩個環境的差異
- 提供環境切換的操作指南

## 🟢 次要缺失（可以在執行中學習）

### 7. Redmine 和 Slack 操作的細節

**缺失資訊：**
- ❌ 如何在 Redmine 建立 Issue？
  - API token 或登入方式
  - Issue 的必填欄位
  - 與主活動單的關聯方式
  
- ❌ 如何在 Slack 發送通知？
  - Bot token 或 Webhook URL
  - 訊息格式要求
  - 討論串的定位方式

**建議補充方式：**
- 提供 Redmine API 的使用範例
- 提供 Slack 通知的 Webhook URL 或 Bot token

### 8. 排程表的更新機制

**缺失資訊：**
- ❌ 排程表的結構和欄位
- ❌ 如何標記活動狀態（進行中、已完成等）
- ❌ 更新排程表的時機

**建議補充方式：**
- 提供排程表的說明文檔
- 列出狀態值的定義
